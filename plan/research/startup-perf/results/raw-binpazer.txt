SUPERSEDED -- LOCAL, NOISY, NOT A REPORTED NUMBER

Taken on the shared development sandbox while other workers were running.
Kept only as the record of how the probes were developed. Every number in
the *.md files beside this one comes from a GitHub Actions runner.

----

# binpazer as a cooked-config trailer

- library: `github.com/wow-look-at-my/bin-file-fmt/go` (MIT)
- blocks: string table 24576 B, rules 2862 B
- container: 27728 B (1.1% overhead over the two payloads)
- iterations: 300, median reported

| operation | us | note |
|---|---|---|
| open reader (header + type table + footer) | 15.7 | NewReaderAt: what every read below starts with |
| open + FindFirst(rules) + ReadPayload | 25.1 | the hot path: only the block the invocation needs |
| open + both blocks | 14.4 | string table plus rules, for the cold path that needs both |

## appended to `/home/user/api-cache/plan/research/startup-perf/bin/go-hello` (1885405 B host + 27728 B container)

| operation | us | note |
|---|---|---|
| os.Executable-style open + last 8 B + section reader + rules block | 20.8 | the whole per-exec cost of a binpazer-cooked binary |
| open + close only | 2.9 | the syscall floor under the row above |


## the same read from C (binpazer's C implementation, allocation-free)

| operation | min us | p50 us | mean us | note |
|---|---|---|---|---|
| C: open + footer + index + rules block | 8.3 | 8.4 | 9.2 | payload 2862 B, allocation-free reader |
| C: open + close only | 1.1 | 1.3 | 1.3 | the syscall floor under the row above |
### binpazer, repeat run (the first run caught a noisy patch of the VM)
|---|---|---|
| open reader (header + type table + footer) | 13.9 | NewReaderAt: what every read below starts with |
| open + FindFirst(rules) + ReadPayload | 15.2 | the hot path: only the block the invocation needs |
| open + both blocks | 13.4 | string table plus rules, for the cold path that needs both |

## appended to `/home/user/api-cache/plan/research/startup-perf/bin/go-hello` (1885405 B host + 27728 B container)

| operation | us | note |
|---|---|---|
| os.Executable-style open + last 8 B + section reader + rules block | 18.0 | the whole per-exec cost of a binpazer-cooked binary |
| open + close only | 2.9 | the syscall floor under the row above |


| operation | min us | p50 us | mean us | note |
|---|---|---|---|---|
| C: open + footer + index + rules block | 8.3 | 8.4 | 8.7 | payload 2862 B, allocation-free reader |
| C: open + close only | 1.1 | 1.2 | 1.2 | the syscall floor under the row above |