These files are from the DEVELOPMENT sandbox, which runs many workers at once.
They are noisy and are superseded by the ci-*/ directories, which come from a
GitHub Actions runner. Kept only so the probes can be smoke-tested offline.
